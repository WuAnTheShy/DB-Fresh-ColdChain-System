# 生鲜冷链团购分销供应链系统 — 技术文档

> 适用读者：对 C# 零基础的组员  
> 目的：看懂项目用了哪些技术、整体架构、代码在哪里  
> 更新日期：2026-08-02

---

## 一、技术栈

| 技术 | 一句话解释 |
|------|-----------|
| **C#** | 编程语言，语法和 Java 90% 相同 |
| **.NET 10** | C# 代码的运行平台 |
| **ASP.NET Core MVC** | Web 框架，处理浏览器请求返回 HTML 页面 |
| **Dapper** | 轻量 ORM，把 C# 对象和 SQL 互相转换 |
| **Oracle 18c** | 数据库 |
| **Oracle.ManagedDataAccess.Core** | Oracle 官方 .NET 驱动 |
| **Razor (.cshtml)** | HTML 里嵌入 C# 代码渲染动态页面 |
| **Bootstrap 5** | 现成 CSS 组件（按钮、表格、导航栏） |
| **jQuery** | 简化前端 JS 代码 |

NuGet 包（`FreshColdChain.csproj`）：

```xml
Dapper 2.1.66
Oracle.ManagedDataAccess.Core 23.26.200
```

---

## 二、三组架构

```
┌─────────────────────────────────────────────┐
│           FreshColdChain (统一项目)          │
├─────────────────────────────────────────────┤
│  A 组：供应商 + 产品 + 库存 + 冷链物流       │
│  9 张表：Inv_* / Biz_PriceRules / Log_*     │
├─────────────────────────────────────────────┤
│  B 组：订单 + 消费者 + 优惠券 + 积分         │
│  8 张表：Biz_Orders / Crm_* / Mkt_*         │
├─────────────────────────────────────────────┤
│  C 组：团长 + 支付 + 退款 + 系统管理          │
│  7 张表：Crm_Promoters / Fin_* / Sys_*      │
└─────────────────────────────────────────────┘
```

各组只操作自己的表。跨组调用通过 `Interfaces/` 下的 C# 接口完成。

---

## 三、分层架构

每个请求的完整路径：

```
浏览器 URL
  → Controller    接请求，调 Service，返回 View
    → Service     判断、计算、校验、控制事务
      → Repository  拼 SQL、查 Oracle
        → Dapper → Oracle
      ← 数据
    ← 结果
  ← .cshtml 渲染成 HTML
浏览器看到页面
```

| 层 | 目录 | 职责 |
|----|------|------|
| 控制层 | `Controllers/` | 接 HTTP 请求，调 Service，返回 View |
| 表现层 | `Views/` | .cshtml 文件，HTML + Razor 混写 |
| 业务层 | `Services/` | 所有计算、校验、事务控制 |
| 数据层 | `Repositories/` | 只负责拼 SQL 和数据库 CRUD |
| 接口层 | `Interfaces/` | 跨组交互契约 + A 组内部接口 |
| 模型层 | `Models/` | 数据库表结构 + DTO |
| DTO | `Models/DTOs/` | 传给页面/跨组的精简数据对象 |

---

## 四、A 组负责的 9 张表

全部主键 `VARCHAR2(36)`，应用层生成 GUID。

| 表 | Model 文件 | 说明 |
|----|-----------|------|
| `Inv_Suppliers` | `Models/InvSupplier.cs` | 供应商（含资质、信用等级） |
| `Inv_Category` | `Models/InvCategory.cs` | 商品分类 |
| `Inv_Products` | `Models/InvProduct.cs` | 商品（含重量、体积、保质期、温区） |
| `Inv_StockSummary` | `Models/InvStockSummary.cs` | 库存汇总（总/锁定/可用） |
| `Inv_StockBatches` | `Models/InvStockBatch.cs` | 库存批次（FEFO 先进先出） |
| `Biz_PriceRules` | `Models/BizPriceRule.cs` | 动态定价规则 |
| `Log_FreightTemplates` | `Models/ColdChainLogistics.cs` | 冷链运费模板 |
| `Log_ExpressDeliveries` | `Models/ColdChainLogistics.cs` | 冷链发货单 |
| `LogFulfillmentBatchItems` | `Models/ColdChainLogistics.cs` | 批次溯源映射 |

---

## 五、A 组 Controller → URL 对照表

| URL | Controller | 功能 |
|-----|-----------|------|
| `/` | `HomeController` | 首页仪表盘 |
| `/Home/Error` | `HomeController` | 错误页 |
| `/Products/Index` | `ProductsController` | 产品列表（搜索+分页） |
| `/Products/Details/{id}` | `ProductsController` | 产品详情 |
| `/Products/Create` | `ProductsController` | 新增产品 |
| `/Products/Edit/{id}` | `ProductsController` | 编辑产品 |
| `/Products/Inventory?productId=` | `ProductsController` | 库存出入库管理 |
| `/Products/LowStock` | `ProductsController` | 低库存预警 |
| `/Suppliers/Index` | `SuppliersController` | 供应商列表 |
| `/Suppliers/Details/{id}` | `SuppliersController` | 供应商详情 |
| `/Suppliers/Create` | `SuppliersController` | 新增供应商 |
| `/Suppliers/Edit/{id}` | `SuppliersController` | 编辑供应商 |
| `/ColdChainLogistics/Index` | `ColdChainLogisticsController` | 运费模板列表 |
| `/ColdChainLogistics/CreateTemplate` | `ColdChainLogisticsController` | 新增运费模板 |
| `/ColdChainLogistics/Quote` | `ColdChainLogisticsController` | 运费报价计算器 |
| `/ColdChainLogistics/Shipments` | `ColdChainLogisticsController` | 发货记录 |
| `/ColdChainLogistics/CreateShipment` | `ColdChainLogisticsController` | 创建发货 |

---

## 六、跨组接口

### A 组提供给 B 组的接口

| 接口 | 方法 | 功能 |
|------|------|------|
| `IInventoryService` | `ReserveAsync(items, tx)` | 预留库存：FOR UPDATE 锁行 → 校验 → 增加 LockedQty → 返回商品快照 |
| `IInventoryService` | `ReleaseAsync(request, tx)` | 释放库存：LockedQty 恢复为可用 |
| `ILogisticsService` | `CalculateFreightAsync(req, tx)` | 阶梯冷链运费计算 |
| `ILogisticsService` | `CreateShipmentAsync(req, tx)` | FEFO 批次扣减 + 发货 + 溯源 |
| `ILogisticsService` | `GetSupplierStatusesAsync(...)` | 查询供应商履约状态 |

### A 组提供给 C 组的接口

| 接口 | 方法 | 功能 |
|------|------|------|
| `IProductInventoryService` | `GetProductStockAsync(id)` | 查商品库存总量 |
| `ISupplierService` | `FindSupplierAccountAsync(...)` | 多条件查供应商账户 |
| `ISupplierService` | `VerifySupplierPasswordAsync(...)` | 验证供应商登录 |
| `ISupplierService` | `DeleteSupplierAsync(id)` | 删除供应商 |

### B 组提供给 A 组的接口

| 接口 | 方法 | 功能 |
|------|------|------|
| `IOrderService` | `CreateOrderAsync` / `GetOrdersAsync` 等 | 订单 CRUD + 状态流转 |

### C 组提供给 B 组的接口

| 接口 | 方法 | 功能 |
|------|------|------|
| `ICommissionService` | `RegisterCompletedOrderAsync` | 订单完成佣金登记（目前用 Dummy） |

---

## 七、核心机制

### 7.1 事务控制（两套模式共存）

**模式 1：A 组自用（UnitOfWork 内部事务）**

```csharp
// ProductInventoryService.StockOutAsync
await _uow.BeginAsync();              // 开事务
var st = await _stockRepo.GetByProductIdForUpdateAsync(id);  // FOR UPDATE 锁行
st.TotalQty -= quantity;
_stockRepo.Update(st);
// ... FEFO 批次扣减 ...
await _uow.CommitAsync();             // 提交
```

**模式 2：B 组调用（外部事务）**

```csharp
// InventoryServiceAdapter.ReserveAsync
_uow.AttachExternalTransaction(transaction);  // 挂载 B 组的事务
var stock = await _stockRepo.GetByProductIdForUpdateAsync(id);  // 走 B 组的连接
stock.LockedQty += quantity;
_stockRepo.Update(stock);
// B 组统一 Commit/Rollback
```

### 7.2 并发防超卖

```sql
SELECT * FROM "Inv_StockSummary" WHERE "ProductID" = :Id FOR UPDATE
```

同一产品的并发请求被行级锁串行化。事务提交或回滚时释放锁。

### 7.3 FEFO 先进先出

出库时按 `ExpiryDate ASC` 排序批次，从最早过期的开始扣减。库存汇总和批次明细在同一事务内更新。

### 7.4 阶梯冷链运费

按省→市→区优先级匹配运费模板，区分冷藏/冷冻/常温三种温区。公式：

```
运费 = 首重费用 + ⌈(总重量 - 首重) / 续重单位⌉ × 续重单价 + 冷链包装费
满足包邮阈值时运费 = 0
```

### 7.5 批次溯源

`LogFulfillmentBatchItems` 表记录每次发货中每个商品扣了哪个批次的多少数量，实现从订单到批次的完整追溯链。

---

## 八、C# 语法速查

```csharp
// 变量
string name = "西兰花";        // 字符串
int count = 100;               // 整数
decimal price = 12.50m;        // 金额（精确小数，.NET 特有）
DateTime now = DateTime.Now;   // 日期时间
var x = "自动推断类型";

// 属性 — C# 特有的自动 getter/setter
public string Name { get; set; } = string.Empty;

// 泛型 — 和 Java 一样
List<Product> products = new();
ApiResponse<ProductDto> result;

// Lambda — 简化写法
items.Select(p => new ProductDto { Name = p.ProductName })

// 异步方法 — 两条规则：
// 1. 调用时加 await
// 2. 方法自己标记 async Task<>
public async Task<IActionResult> Details(string id)
{
    var result = await _service.GetProductByIdAsync(id);  // await
    return View(result.Data);
}
```

---

## 九、ASP.NET Core MVC 关键注解

| 注解 | 用途 |
|------|------|
| `[HttpGet]` | 标记浏览器输入 URL 时调用 |
| `[HttpPost]` | 标记表单提交时调用 |
| `[ValidateAntiForgeryToken]` | 防跨站伪造请求 |
| `IActionResult` | 返回类型：可返回页面/跳转/404 |
| `return View(data)` | 把数据传给 .cshtml 渲染 |
| `return RedirectToAction("Index")` | 跳转 |
| `TempData["Success"] = msg` | 跳转后显示的一次性消息 |

---

## 十、Dapper 基本操作

```csharp
// 查一条
var sql = """SELECT * FROM "Inv_Products" WHERE "ProductID" = :Id""";
var p = await conn.QuerySingleOrDefaultAsync<InvProduct>(sql, new { Id = "xxx" });

// 查列表
var list = await conn.QueryAsync<InvProduct>("SELECT * FROM \"Inv_Products\"");

// 插入（VARCHAR2 PK — 应用层生成 GUID）
var sql = """INSERT INTO "Inv_Suppliers" ("SupplierID", "SupplierName", ...)
              VALUES (:SupplierID, :SupplierName, ...)""";
await conn.ExecuteAsync(sql, supplier);

// 更新
var sql = """UPDATE "Inv_Products" SET "ProductName" = :Name WHERE "ProductID" = :Id""";
await conn.ExecuteAsync(sql, new { Name = "新名字", Id = "xxx" });

// 删除
var sql = """DELETE FROM "Inv_Suppliers" WHERE "SupplierID" = :Id""";
await conn.ExecuteAsync(sql, new { Id = "xxx" });
```

Oracle 注意点：
- 参数前缀用 `:` 不用 `@`
- 表名列名加双引号避免关键字冲突
- 分页用 `OFFSET ... ROWS FETCH NEXT ... ROWS ONLY`

---

## 十一、BaseRepository 泛型基类

A 组的核心基础设施。所有 Repository 继承它，自动获得 CRUD 能力：

```csharp
public class ProductRepository : BaseRepository<InvProduct>, IProductRepository
{
    // 继承的通用方法（自动生成 SQL）：
    //   GetByIdAsync(string id)
    //   GetAllAsync()
    //   GetPagedAsync(pageIndex, pageSize)
    //   CountAsync()
    //   AddAsync(entity)      ← 自动生成 GUID
    //   Update(entity)
    //   Delete(entity)

    // 特殊查询手写 SQL（如多表 JOIN）
    public async Task<InvProduct?> GetByIdWithDetailsAsync(string id) { ... }
}
```

原理：`BaseRepository<T>` 通过反射读取 `[Table]`、`[Column]`、`[Key]` 特性，自动拼接 `INSERT/UPDATE/DELETE/SELECT` SQL。支持 `int` 自增和 `VARCHAR2(36)` GUID 两种主键。

---

## 十二、Bootstrap 快速参考

```html
<!-- 按钮 -->
<a class="btn btn-primary">蓝</a>
<a class="btn btn-success">绿</a>
<a class="btn btn-danger">红</a>
<a class="btn btn-outline-secondary">灰边框</a>

<!-- 表格 -->
<table class="table table-striped table-hover">
  <thead class="table-dark">...</thead>
</table>

<!-- 栅格（一行 12 列） -->
<div class="row">
  <div class="col-md-4">占 4/12</div>
  <div class="col-md-8">占 8/12</div>
</div>

<!-- 卡片 -->
<div class="card">
  <div class="card-header">标题</div>
  <div class="card-body">内容</div>
</div>

<!-- 警告 -->
<div class="alert alert-success">成功</div>
<div class="alert alert-danger">失败</div>

<!-- 表单 -->
<form asp-action="Create" method="post">
  @Html.AntiForgeryToken()
  <input asp-for="Name" class="form-control" required />
  <button type="submit" class="btn btn-success">提交</button>
</form>
```

---

## 十三、Git 分支

```
main ──────────── 主干（大组长合并）
  ├── dev-groupA ── A 组（你现在在这里）
  ├── dev-groupB ── B 组
  └── dev-groupC ── C 组
```

日常操作：
```bash
git pull origin dev-groupA    # 拉最新
git add -A                    # 暂存
git commit -m "feat: xxx"     # 提交
git push origin dev-groupA    # 推送
```

---

## 十四、文件快速索引

| 你想找什么 | 去哪里 |
|-----------|--------|
| 启动入口 | `Program.cs` |
| 数据库连接 | `appsettings.json` |
| A 组表结构 | `Models/Inv*.cs`、`Models/BizPriceRule.cs`、`Models/ColdChainLogistics.cs` |
| 通用 CRUD | `Repositories/BaseRepository.cs` |
| 事务管理 | `Repositories/UnitOfWork.cs` |
| 行级锁 | `Repositories/StockSummaryRepository.cs` → `GetByProductIdForUpdateAsync` |
| FEFO 算法 | `Services/ProductInventoryService.cs` → `StockOutAsync` |
| 运费算法 | `Services/ColdChainLogisticsService.cs` → `QuoteFreightAsync` |
| B 组库存适配器 | `Services/InventoryServiceAdapter.cs` |
| B 组物流适配器 | `Services/LogisticsServiceAdapter.cs` |
| C 组佣金 Dummy | `Services/DummyCommissionService.cs` |
| B 组跨组接口 | `Interfaces/B组_*.cs` |
| 跨组契约模型 | `Models/CrossGroup/CrossGroupContracts.cs` |
| 页面模板 | `Views/` 目录 |
| 传给页面的数据 | `Models/DTOs/` 目录 |
| 跨组接口文档 | `Interfaces/README.md` |
| NuGet 包配置 | `FreshColdChain.csproj` |
| 全长技术文档 | `技术文档.md`（本文档） |

# 生鲜冷链团购分销供应链系统 — 技术文档

> 适用读者：对 C# 零基础的组员  
> 目的：看懂项目用了哪些技术，每项技术干什么，代码放在哪里

---

## 一、技术栈总览

| 技术 | 分类 | 一句话解释 |
|------|------|-----------|
| C# | 编程语言 | 写后端逻辑的语言，语法和 Java 很像 |
| .NET 10 | 运行平台 | C# 代码的"操作系统"，负责编译和运行 |
| ASP.NET Core MVC | Web 框架 | 处理浏览器请求，返回 HTML 页面 |
| Dapper | 数据库工具 | 把 C# 对象和 SQL 语句互相转换 |
| Oracle 18c | 数据库 | 存数据的地方 |
| Razor (.cshtml) | 模板引擎 | 在 HTML 里嵌入 C# 代码，生成动态页面 |
| Bootstrap 5 | CSS 框架 | 现成的按钮、表格、导航栏样式 |
| jQuery | JS 库 | 简化前端交互代码 |
| Git | 版本控制 | 代码协作和备份 |
| NuGet | 包管理器 | 下载第三方库（类似 npm） |

---

## 二、C# — 编程语言

### 是什么

微软开发的编程语言，和 Java 同属 C 语言家族。如果你学过 Java，C# 几乎不需要额外学习——关键字、语法结构 90% 相同。

### 在项目里干什么

所有 `.cs` 文件都是 C# 代码。定义类、写方法、调数据库、处理请求。

### 关键语法速查

```csharp
// 变量声明
string name = "西兰花";        // 字符串
int count = 100;               // 整数
decimal price = 12.50m;        // 金额（精确小数）
DateTime now = DateTime.Now;   // 日期时间
var result = "自动推断类型";    // var = 让编译器自己判断类型

// 类和方法
public class ProductService     // 定义一个类
{
    public async Task<ApiResponse<ProductDto>> GetProductAsync(string id)  // 定义一个异步方法
    {
        // 方法体
        return ApiResponse<ProductDto>.Success(data);
    }
}

// 属性（Property）— C# 特有的"智能字段"
public string Name { get; set; }  // 自动生成 getter/setter，等同于 Java 的 get/set 方法

// 泛型 — 和 Java 的泛型一样
List<Product>          // Product 类型的列表
ApiResponse<Product>   // 包装 Product 的返回结果

// Lambda 表达式 — 简化写法
items.Select(p => new ProductDto { ... })   // 对每个元素做转换
```

### 对应的文件

```
所有的 .cs 文件
```

---

## 三、.NET 10 — 运行平台

### 是什么

C# 代码的运行时环境。类似于 Java 的 JVM——你写的 C# 代码编译成中间语言（IL），.NET 在服务器上执行它。

### 在项目里干什么

- `dotnet build` → 编译代码
- `dotnet run` → 启动网站
- NuGet 包管理 → 下载第三方库
- 依赖注入 → 自动管理对象的创建和销毁

### 对应的文件

```
FreshGroupSystem.csproj    — 项目配置（用什么包、目标框架）
Program.cs                 — 启动入口
```

---

## 四、ASP.NET Core MVC — Web 框架

### 是什么

.NET 自带的 Web 开发框架。MVC = Model-View-Controller 三层分离。

### 三个部分各干什么

```
浏览器请求 /Products/Details/abc-123
         │
    ┌────▼─────┐
    │Controller│  接收请求，调用业务层，决定返回哪个页面
    │  控制层   │  只管流程，不写业务逻辑
    └────┬─────┘
         │ 调用
    ┌────▼─────┐
    │  Model   │  数据和业务规则
    │  模型层   │  = Entity（数据库表结构）+ DTO（传给页面的数据）+ Service（计算和校验）
    └────┬─────┘
         │ 返回数据
    ┌────▼─────┐
    │  View    │  把数据渲染成 HTML
    │  视图层   │  .cshtml 文件 = HTML + C# 混写
    └──────────┘
         │
    浏览器看到页面
```

### URL 怎么映射到代码

```
路由规则：{controller}/{action}/{id?}

/Products/Details/abc-123  →  ProductsController.Details("abc-123")
/Suppliers/Create           →  SuppliersController.Create()        (GET 请求 = 显示表单)
/Suppliers/Create           →  SuppliersController.Create(dto)     (POST 请求 = 提交表单)
```

### 关键概念

| 概念 | 干什么用 |
|------|---------|
| `[HttpGet]` | 标记"浏览器输入 URL 访问时"调这个方法 |
| `[HttpPost]` | 标记"表单提交时"调这个方法 |
| `[FromBody]` | 从请求体里读取 JSON 数据 |
| `[FromQuery]` | 从 URL 参数里读取数据（如 `?pageIndex=1`） |
| `IActionResult` | 方法的返回类型——可以返回页面、跳转、404 等 |
| `return View(data)` | 把数据传给 .cshtml 页面渲染 |
| `return RedirectToAction("Index")` | 跳转到另一个页面 |
| `return NotFound()` | 返回 404 |
| `[ValidateAntiForgeryToken]` | 防止跨站伪造请求（安全机制） |
| `TempData["Success"]` | 一次性的跨请求传消息（跳转后显示"操作成功"） |

### 对应的文件

```
Controllers/HomeController.cs
Controllers/ProductsController.cs
Controllers/SuppliersController.cs
Controllers/ColdChainLogisticsController.cs
Program.cs                         — MVC 的配置和启动
```

---

## 五、Dapper — 数据库访问工具

### 是什么

一个轻量级的"对象-SQL 映射"工具。你把 SQL 写好后，Dapper 帮你把查询结果自动转成 C# 对象。

### 为什么不直接用 SQL

你可以手写 `OracleCommand`、`OracleDataReader`，一行一行读数据，手动赋值到对象属性——但那样要写几百行重复代码。Dapper 帮你省掉了这步。

### 在项目里怎么用的

```csharp
// 查一条
var sql = """SELECT * FROM "Inv_Products" WHERE "ProductID" = :Id""";
var product = await connection.QuerySingleOrDefaultAsync<Product>(sql, new { Id = "abc-123" });

// 查列表
var sql = """SELECT * FROM "Inv_Suppliers" """;
var list = await connection.QueryAsync<Supplier>(sql);

// 插入
var sql = """INSERT INTO "Inv_Suppliers" (...) VALUES (...) """;
await connection.ExecuteAsync(sql, supplierObject);

// 更新
var sql = """UPDATE "Inv_Products" SET "ProductName" = :Name WHERE "ProductID" = :Id""";
await connection.ExecuteAsync(sql, new { Name = "新名字", Id = "abc-123" });
```

### 对应的文件

```
Repositories/BaseRepository.cs    — 通用 CRUD，反射生成 SQL（核心）
Repositories/SupplierRepository.cs
Repositories/ProductRepository.cs
Repositories/StockSummaryRepository.cs
Repositories/StockBatchRepository.cs
Repositories/CategoryRepository.cs
Repositories/PriceRuleRepository.cs
```

---

## 六、Oracle 数据库 — 数据存储

### 用什么连

`Oracle.ManagedDataAccess.Core` — Oracle 官方提供的 .NET 数据库驱动。不需要安装 Oracle 客户端，一个 NuGet 包搞定。

### 连接字符串在哪里

```json
// appsettings.json
"ConnectionStrings": {
    "DefaultConnection": "User Id=COLDCHAIN;Password=Oracle123;Data Source=111.231.80.220:1521/XE;"
}
```

### Dapper 怎么和 Oracle 配合

```
C# 代码 → Dapper → Oracle.ManagedDataAccess.Core → 网络 → Oracle 数据库
```

Oracle 的 SQL 语法特殊点：

| 注意点 | 说明 |
|--------|------|
| 参数用 `:name` | 不能用 `@name`（那是 SQL Server 的） |
| 标识符加双引号 | `"Inv_Products"` 避免关键字冲突 |
| 分页用 `OFFSET...FETCH` | `OFFSET 0 ROWS FETCH NEXT 10 ROWS ONLY` |
| `VARCHAR2` 不是 `VARCHAR` | Oracle 特有类型 |

### 对应的文件

```
Repositories/OracleDbConnectionFactory.cs   — 创建数据库连接
appsettings.json                            — 连接字符串
```

---

## 七、依赖注入（DI）— 对象管理器

### 是什么

你不用自己 `new` 对象，框架自动帮你创建和传递。需要什么在构造函数里声明就行。

### 为什么用它

```csharp
// 不用 DI — 你要手动管理所有依赖
var connectionFactory = new OracleDbConnectionFactory(config);
var uow = new UnitOfWork(connectionFactory);
var repo = new ProductRepository(uow);
var service = new ProductInventoryService(repo, ...);
var controller = new ProductsController(service);
// ↑ 每次都要写这一大串，依赖一改全部要改

// 用 DI — 在 Program.cs 注册一次，之后框架自动处理
builder.Services.AddScoped<IProductRepository, ProductRepository>();
// ↑ 之后任何地方写 new ProductController(IProductRepository repo) 框架自动注入
```

### 在项目里的注册

```csharp
// Program.cs
builder.Services.AddScoped<ISupplierService, SupplierService>();
// ↑ 当有人需要 ISupplierService 时，给一个 SupplierService 实例
// ↑ AddScoped = 每个 HTTP 请求创建一个新实例，请求结束自动销毁
```

### 对应的文件

```
Program.cs    — 所有服务的注册
```

---

## 八、异步编程（async/await）

### 是什么

让程序在等待数据库返回结果时，不阻塞线程，转去处理其他请求。

### 怎么读

```csharp
public async Task<ApiResponse<ProductDto>> GetProductAsync(string id)
//     ↑          ↑                       ↑
//   异步标记   返回类型要包 Task<>      方法体里一定有 await

var result = await GetProductAsync("abc-123");
//          ↑
//    等这个方法执行完，拿到结果再继续往下走
```

### 两条规则

1. 调用异步方法时前面加 `await`
2. 用了 `await` 的方法自己也要标记 `async`

### 对应的文件

```
几乎所有方法都是 async Task
```

---

## 九、事务与并发锁 — 数据一致性

### 事务（Transaction）

把多个数据库操作打包——要么全成功，要么全失败。

```csharp
try
{
    await _uow.BeginAsync();           // 开始事务
    await _productRepo.AddAsync(产品);
    await _stockRepo.AddAsync(库存);
    await _uow.CommitAsync();          // 提交：两个操作同时生效
}
catch
{
    await _uow.RollbackAsync();        // 回滚：任一失败，全部撤销
}
```

### 行级锁（FOR UPDATE）

防止并发超卖——多个用户同时操作同一商品库存时，一个个排队处理。

```sql
SELECT * FROM "Inv_StockSummary" WHERE "ProductID" = :Id FOR UPDATE
--         ↑ 这行数据被锁住，其他请求必须等这个事务提交后才能读
```

### 对应的文件

```
Repositories/UnitOfWork.cs               — 事务管理
Repositories/StockSummaryRepository.cs    — FOR UPDATE 实现
Services/ProductInventoryService.cs       — 事务使用（入库/出库）
Services/ColdChainLogisticsService.cs     — 事务使用（发货）
```

---

## 十、FEFO 算法 — 库存扣减策略

### 是什么

**F**irst **E**xpired, **F**irst **O**ut — 最早过期的批次先卖出去。

### 项目里的实现

```csharp
// 出库时
var batches = await _batchRepo.GetByProductIdAsync(productId);
// ↑ 已经按 ExpiryDate ASC 排序（离过期最近的排前面）

foreach (var batch in batches)
{
    var deduct = Math.Min(需要扣的数量, 这个批次的当前库存);
    batch.CurrentQty -= deduct;     // 扣减
    if (batch.CurrentQty == 0)
        batch.Status = "DEPLETED";  // 用完了
    需要扣的数量 -= deduct;
    if (需要扣的数量 == 0) break;    // 扣够了
}
```

### 对应的文件

```
Repositories/StockBatchRepository.cs  — 按过期时间 ASC 查批次
Services/ProductInventoryService.cs   — StockOutAsync（出库扣减）
Services/ColdChainLogisticsService.cs — CreateShipmentAsync（发货扣减）
```

---

## 十一、阶梯运费算法

### 业务逻辑

根据目的地（省/市/区）、商品温区（冷藏/冷冻/常温）匹配运费模板，按"首重 + 续重 + 冷链包装费"计算。

### 匹配优先级

```
精确匹配省/市/区 > 精确省/市 + * > 精确省 + * + * > 全国通配 * + * + *
    得分 7              得分 6           得分 4           得分 0
```

### 计算公式

```
运费 = 首重费用 + ⌈(总重量 - 首重) / 续重单位⌉ × 续重单价 + 冷链包装费
      ↑ 如果有包邮阈值且商品金额 ≥ 阈值，运费 = 0
```

### 对应的文件

```
Services/ColdChainLogisticsService.cs  — QuoteFreightAsync
Views/ColdChainLogistics/Quote.cshtml   — 运费报价页面
```

---

## 十二、前端技术 — 页面长什么样

### Bootstrap 5

一个 CSS 框架，提供现成的组件样式。你不需要自己写 CSS：

```html
<!-- 按钮 -->
<a class="btn btn-primary">蓝色按钮</a>
<a class="btn btn-success">绿色按钮</a>
<a class="btn btn-danger">红色按钮</a>

<!-- 表格 -->
<table class="table table-striped table-hover">  <!-- 斑马纹 + 悬停高亮 -->
    <thead class="table-dark">  <!-- 深色表头 -->
        <tr><th>名称</th><th>价格</th></tr>
    </thead>
    <tbody>
        <tr><td>西兰花</td><td>¥12.50</td></tr>
    </tbody>
</table>

<!-- 栅格系统 — 一行 12 列 -->
<div class="row">
    <div class="col-md-4">占 4/12</div>
    <div class="col-md-8">占 8/12</div>
</div>

<!-- 卡片 -->
<div class="card">
    <div class="card-header">标题</div>
    <div class="card-body">内容</div>
</div>

<!-- 警告框 -->
<div class="alert alert-success">操作成功</div>
<div class="alert alert-danger">操作失败</div>
```

### Razor (.cshtml)

HTML 里嵌入 C# 代码：

```html
@model ProductDto                            ← 声明这个页面接收什么类型的数据

<h1>@Model.ProductName</h1>                  ← 输出属性值
<p>价格：¥@Model.DefaultPrice.ToString("F2")</p>

@if (Model.Status == "ACTIVE")               ← if 判断
{
    <span class="badge bg-success">上架</span>
}
else
{
    <span class="badge bg-secondary">下架</span>
}

@foreach (var item in Model.Items)           ← 循环
{
    <tr><td>@item.Name</td><td>@item.Price</td></tr>
}

<form asp-action="Create" method="post">     ← 表单（自动加防伪标记）
    <input asp-for="ProductName" class="form-control" />
    <button type="submit">提交</button>
</form>
```

### jQuery

简化 JavaScript 代码：

```javascript
// 原生 JS
document.getElementById('addRow').addEventListener('click', function() { ... });

// jQuery（更短）
$('#addRow').on('click', function() { ... });
```

### 对应的文件

```
Views/Shared/_Layout.cshtml       — 所有页面的框架（导航栏 + 页脚）
Views/Shared/_Pagination.cshtml   — 分页组件
Views/Shared/Error.cshtml         — 错误页
Views/Home/Index.cshtml           — 首页
Views/Products/*.cshtml           — 产品模块 6 个页面
Views/Suppliers/*.cshtml          — 供应商模块 4 个页面
Views/ColdChainLogistics/*.cshtml — 冷链物流模块 5 个页面
wwwroot/css/site.css              — 自定义样式
wwwroot/js/site.js                — 自定义脚本
wwwroot/lib/bootstrap/            — Bootstrap 库文件
wwwroot/lib/jquery/               — jQuery 库文件
```

---

## 十三、Git — 版本控制

### 分支结构

```
main ────────────────────────────── 主干（大组长合并）
  │
  ├── dev-groupA ──────── 我们 A 组（你在这里）
  ├── dev-groupB ──────── B 组（订单）
  └── dev-groupC ──────── C 组（团长）
```

### 日常操作

```bash
git pull origin dev-groupA     # 拉取最新代码
git add -A                     # 暂存所有修改
git commit -m "feat: 做了什么"  # 提交到本地
git push origin dev-groupA     # 推送到 GitHub
```

---

## 十四、项目文件快速索引

| 你想找什么 | 去哪里看 |
|-----------|---------|
| 所有 NuGet 包 | `FreshGroupSystem.csproj` |
| 启动配置 | `Program.cs` |
| 数据库连接 | `appsettings.json` |
| URL → 代码映射 | `Controllers/` 目录 |
| 业务规则 | `Services/` 目录 |
| SQL 语句 | `Repositories/` 目录 |
| 数据库表结构 | `Models/` 目录（带 `[Table]` 的类） |
| 传给页面的数据格式 | `Models/DTOs/` 目录 |
| 页面模板 | `Views/` 目录 |
| 跨组接口 | `Interfaces/` 目录 + `README.md` |
| 通用基类 | `Repositories/BaseRepository.cs` |
| 事务代码 | `Repositories/UnitOfWork.cs` |
| FEFO 算法 | `Services/ProductInventoryService.cs` → `StockOutAsync` |
| 运费算法 | `Services/ColdChainLogisticsService.cs` → `QuoteFreightAsync` |
| 并发锁 | `Repositories/StockSummaryRepository.cs` → `GetByProductIdForUpdateAsync` |

-- ============================================================
-- 团长带货介绍「图文并茂」改造 —— 数据库变更（2026-09-03）
-- 数据库: Oracle 18c
--
-- 变更内容（结构无变化，仅列语义变更）：
--   团长带货介绍由“纯文字”升级为“标题 + 多段（文字 / 图片）”的图文介绍。
--   CRM_PRODUCT_ENTRIES.PROMOTERDESC 不再直接保存介绍文字，
--   而是保存图文内容文件的【相对路径】，图文内容以 JSON 文件存放于
--   站点 wwwroot/uploads/promoter-desc/ 下（目录由团长端首次保存时自动创建）。
--
--   存储值约定：
--     空值                        -> 无图文介绍；
--     /uploads/promoter-desc/desc_*.json  -> 图文内容文件相对路径（新格式）；
--     其他非空字符串              -> 历史纯文字介绍（兼容读取：自动包装为单段文字，
--                                    团长首次在详情页保存图文介绍后转为相对路径）。
--
-- 团长端新提供的“上传本地图片”保存至 wwwroot/uploads/promoter-img/（程序自动创建目录）。
-- ============================================================

-- 更新列注释，说明新语义（无需 ALTER TABLE，长度 2000 足以存放相对路径）
COMMENT ON COLUMN CRM_PRODUCT_ENTRIES.PromoterDesc
    IS '团长带货介绍存储值：空=无介绍；/uploads/promoter-desc/*.json=图文内容文件相对路径；其他=历史纯文字（兼容展示，保存图文后转为路径）';

COMMIT;

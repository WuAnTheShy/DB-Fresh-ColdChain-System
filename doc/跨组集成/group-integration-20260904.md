# A/C 最新分支合入 B 组记录

## 合并基线

- B 组修复基线：`630aff2`。
- A 组最新远程提交：`988f9db`，合并前已是 B 组祖先，无需重复合并。
- C 组最新远程提交：`512b777`，以真实 merge 合入 B 组，保留提交祖先关系。
- 只向 `dev-groupB` 推送，不修改 A/C/main 分支。

## 冲突处理

- B 组购物车、个人中心、团长页、订单页保留当前版本，包括移动端布局、注销、取消订单和时间线改进。
- 保留 `630aff2` 的物流状态、收货校验、退款外部事务参数及回归测试，不回退到 C 分支中的旧副本。
- 商品详情页接入 C 组推文入口，去除自动合并产生的重复分类样式变量。
- 团长目录接入富文本摘要转换；去除 `PromoterService` 自动合并产生的重复 `PublishedAt` 初始化。
- `.vs` 本地缓存保留 B 组基线，不把其他开发环境缓存冲突混入提交。
- `wwwroot/app` 使用合并后的源码重新构建，不混用两组旧产物。

## 验证及部署注意

- Release 后端构建：0 警告、0 错误。
- Vue/Vite 生产构建通过。
- 60 个现有自动化业务场景全部通过，B 组边界及 DDL 静态检查通过。
- 本轮未向共享 Oracle 执行迁移或业务写入。富文本迁移仅更新列注释，不改变结构；历史纯文字介绍继续兼容读取。
- 团长推文的 JSON 和图片使用 C 组既有文件存储方式，部署时需保留上传目录。
- 该合并证明代码集成和编译回归通过，不代表退款事务、真实物流、权限等剩余问题已修复，也不替代浏览器和隔离数据库端到端验收。

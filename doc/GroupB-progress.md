# GroupB 完成情况

更新日期：2026-07-27

## 1. 阶段进度

| 阶段 | 状态 | 结果 |
| --- | --- | --- |
| 阶段 1：事务底座与基线审计 | 已完成 | 修复事务连接复用，完成需求差距和分期计划 |
| 阶段 2：真实下单与营销闭环 | 待开始 | - |
| 阶段 3：客户与营销管理 | 待开始 | - |
| 阶段 4：订单查询与状态流转 | 待开始 | - |
| 阶段 5：退款协同与交付验证 | 待开始 | - |

## 2. GroupB 需求矩阵

| 领域 | 要求 | 当前完成情况 | 状态 |
| --- | --- | --- | --- |
| 消费者 | 消费者基础信息管理 | 已有模型、按 ID 查询仓储和 Service；缺少新增、编辑、页面 | 部分完成 |
| 地址 | 收货地址管理 | 已有模型和列表查询；缺少 CRUD、默认地址唯一约束和页面 | 部分完成 |
| 会员 | 动态等级与成长体系 | 已有按累计消费匹配等级的方法；尚未持久化自动升级 | 部分完成 |
| 优惠券 | 券定义与用户券记录 | 已有查询、模板读取、库存扣减和核销仓储 | 部分完成 |
| 防薅羊毛 | 归属、状态、有效期、门槛、并发校验 | 查询包含部分过滤，Service 仅校验门槛；未形成事务闭环 | 未完成 |
| 积分 | 发放、扣回和不可缺失流水 | 已有下单发放、退款扣回及流水写入；缺少幂等和边界规则 | 部分完成 |
| 订单 | 订单主表和明细创建 | 已有事务服务和仓储；Controller 明细仍为硬编码演示 | 部分完成 |
| 订单事务 | 全局提交或回滚 | 阶段 1 已确保事务内所有 Repository 操作复用同一连接 | 已完成 |
| 供应商拆单 | 按供应商拆分明细/履约单元 | 明细已有 `SupplierId`，无拆单逻辑和展示 | 未完成 |
| 跨组库存 | 通过 A 组接口校验和扣减库存 | 尚未冻结 GroupB 所需契约和 Dummy | 未完成 |
| 退款协同 | 扣回积分并通知 C 组撤销预计佣金 | 仅有积分扣回；缺少幂等和 C 组接口 | 未完成 |
| Web 页面 | 单一入口、Bootstrap 统一界面 | 仅有下单演示页和空详情页 | 未完成 |
| 数据库 | Oracle DDL、约束、种子数据 | 有 8 表草案；与总设计主键类型不一致，未执行集成验证 | 部分完成 |
| 测试 | 单元、事务、Oracle 集成和页面冒烟测试 | 当前完成编译与 Kestrel 启动级检查 | 未完成 |

## 3. 阶段 1 变更记录

- `BaseRepository` 新增统一连接执行入口：
  - 有事务时复用 `transaction.Connection`；
  - 无事务时自行创建、打开和释放连接；
  - 拒绝使用已结束或连接已关闭的事务。
- 所有 GroupB Repository 已改为通过统一入口执行 Dapper 操作。
- 订单创建和退款扣积分中的消费者查询已加入同一事务。
- 下单遇到消费者不存在时会抛出异常并回滚，不再生成缺少积分资产闭环的订单。
- 新建 `doc` 目录下的实施计划、进度文档和阶段总结。

## 4. 当前验证结果

- `dotnet build --nologo`：通过，0 个警告、0 个错误。
- `dotnet run --no-build --no-launch-profile --urls http://localhost:53124`：
  Kestrel 成功启动并监听本地端口，验证后已停止进程。
- `git diff --check`：通过。
- Repository 连接调用审计：除 `BaseRepository` 外，无 Repository 自行创建事务外连接。
- Oracle 集成测试：本阶段未执行，原因是仓库中没有隔离测试库说明，不能在未知共享库写入测试订单。

## 5. 下一阶段入口

阶段 2 首先冻结真实下单请求和 A 组库存接口，再完成服务端金额计算、优惠券核销、
会员积分倍率和异常回滚场景。不得继续使用 Controller 中的硬编码商品明细。

# 接口检查与 B 组修复记录

日期：2026-09-04。工作分支：`dev-groupB`。

## 本阶段修复范围

1. `FallbackGroupALogisticsExtensionProvider`：基础 `SHIPPED` 不再覆盖扩展物流状态；基础发货单查询后缓存快照，支持稳定事件编号和继续追加轨迹。
2. `OrderService`、`OrdersApiController`：消费者确认收货检查所属包裹已签收；自动收货和管理端完成检查全部包裹已签收。订单状态与响应中的操作标记共同约束入口。
3. `IOrderService`、`OrderService`：全额及部分退款支持可选外部事务。B 组不提交、回滚或释放调用方事务；异常传播给调用方。旧调用方式仍兼容，但不因此获得跨组原子性。
4. 场景测试：验证运输状态刷新、基础发货单恢复、未签收拒绝、自动及管理端完成条件、外部退款事务提交/回滚及写入失败时的事务归属。

## 后续授权修复进展

用户要求确认 A/C 最新代码合入 B 后授权跨组修复。合并提交 `54598f4` 已推送，A=`988f9db`、C=`512b777` 均为 B 分支祖先。

- C 组退款调用已传当前事务给 B 组，全额及部分退款不再独立提交积分和订单。
- 支付、退款记录及佣金撤销审计已共用当前事务；审计仓储在提供事务时使用该事务连接，不自建连接、提交或释放它。
- 佣金撤销失败、佣金状态/已退金额/比例更新失败均中止退款，避免忽略失败继续批准。
- 新增 7 个场景覆盖真实 C/B 业务服务之间的事务传递、审核最后一步失败、直接退款审计失败、支付自有与外部事务以及佣金失败传播。总计 67 个场景通过，Release 编译 0 警告/0 错误，边界及 DDL 静态检查通过。
- 以上回归使用内存仓储；隔离 Oracle 原子性和浏览器端到端验证仍未完成，不据此宣称全部联调完成。
- A 组报价已按供应商、温区、模板合并重量，B 组报价与 C 组退款报价均传递订单供应商快照；新增 7 个场景后累计 74 个场景通过。只影响新报价，不重算历史订单快照。
- B 组订单及履约入口已调用 C 组统一授权服务，逐动作区分读写权限并检查当前账号/角色/供应商资质状态。新增 8 个授权场景后累计 82 个场景通过；配置和旧会话重登说明见正式接口文档第 4 节。

## 仍未完成的跨组事项

| 项目 | 当前证据 | 后续验收要求 |
| --- | --- | --- |
| 财务事务真实数据库验收 | 代码及内存仓储回归已修复，未执行 Oracle 集成测试 | 在隔离 schema 验证退款记录、积分、订单、佣金和审计原子提交与回滚 |
| 运费真实数据联调 | 算法与混合商品回归已修复，未验证共享库全部模板数据 | 使用隔离环境验证真实商品重量、供应商、温区与模板配置，比较结算及退款前后报价 |
| A 组高级物流 | DI 仍注册内存 `FallbackGroupALogisticsExtensionProvider` | A 组正式持久化实现及迁移；验证事务回滚、进程重启后轨迹保留、事件幂等 |
| 权限真实账号验收 | B 组入口已接统一授权，服务与过滤器拒绝场景通过 | 核对部署数据库角色 ID 的权限映射，使用实际登录会话验证读写、停用、供应商资质及越权拒绝 |
| C 组富文本介绍端到端验证 | 最新功能已合入 B，合并构建通过；未推送 main | 浏览器验证已有目录进入推文、刷新、商品切换及内容读取链路 |
| 结构化物流退款证据 | 当前支持物流责任类型，未接入结构化轨迹证据 | 与 C 组确认契约后验证证据归属、可信来源及审核使用 |

第一阶段仅修改 B 组；后续经用户明确授权修改必要的跨组实现，仍只在 B 分支提交，没有向 A/C/main 分支推送，也没有对共享数据库执行迁移或业务写入。

## 验证边界

- 本阶段实际验证：Release 编译 0 警告、0 错误；60 个自动化场景全部通过；B 组职责边界、DDL 静态检查及 `git diff --check` 通过。未改动前端源码，本阶段未重新执行前端构建。
- 自动化场景使用内存 Fake；不能证明真实 Oracle 跨组事务已贯通。
- 兜底数据仍只存在于进程内。恢复基础发货单不等于恢复重启前的扩展轨迹，后者仍会丢失；真实物流持久化未完成。
- 本轮不执行共享 Oracle 写入验证。正式验收需要隔离测试 schema 和端到端业务回归。
- 分支快照：A=`988f9db`，B 基线=`67b9d5c`，C=`512b777`，main=`0ae0fc7`。B 比 main 多 9 个提交；A 提交已进入 B/main；C 最新介绍功能未进入 B/main。
